<view class="wrapper">
  <view class="image" wx:for="{{images}}" wx:key="{{item.id}}">
    <image class="img" src="{{item.url}}"></image>
  </view>
  <view class="image image-plus">
    <image class="img" src="./plus.png"></image>
  </view>
</view>